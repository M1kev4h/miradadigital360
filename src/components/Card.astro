---
const { title, description, videoSrc } = Astro.props;
---
<div class="flex flex-col md:flex-row md:ml-16  md:w-[700px]   overflow-hidden mt-16 ">
  <div class="md:w-1/3">
    <video class="w-[200px] rounded-xl"  autoplay="" loop muted transition:persist> <source src={videoSrc} type="video/mp4" /> Tu navegador no soporta la reproducción de este video. </video>
  </div>
  <div class="flex-col  p-0 md:w-2/3">
    <h3 class="text-4xl text-[#e1fd6f] font-semibold mb-2">{title}</h3>
    <p class="mt-8 text-white text-xl">{description}</p>
  </div>
</div>
